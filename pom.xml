<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/>
	</parent>
	<groupId>org.apiaddicts.apitools.openapi2soapui</groupId>
	<artifactId>openapi2soapui</artifactId>
	<version>2.1.0</version>
	<packaging>pom</packaging>

	<name>openapi2soapui</name>
	<description>Openapi to SoapUI Project</description>
	<url>https://github.com/apiaddicts/openapi2soapui</url>

	<licenses>
		<license>
			<name>GNU Lesser General Public License (LGPLV3+)</name>
			<url>http://www.gnu.org/licenses/lgpl-3.0.html</url>
		</license>
	</licenses>

	<organization>
		<name>Apiaddicts</name>
		<url>https://apiaddicts.org</url>
	</organization>

	<developers>
		<developer>
			<id>MH</id>
			<name>Melsy Huamani</name>
			<email>melsy.huamani.pe@cloudappi.net</email>
			<organization>Cloudappi</organization>
		</developer>
		<developer>
			<id>SD</id>
			<name>Sebastian Diaz</name>
			<email>sebastian.diaz.pe@cloudappi.net</email>
			<organization>Cloudappi</organization>
		</developer>
		<developer>
			<id>RG</id>
			<name>Rafael Goterris</name>
			<email>rafael.goterris@cloudappi.net</email>
			<organization>Cloudappi</organization>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:git://github.com/apiaddicts/openapi2soapui.git</connection>
		<developerConnection>scm:git:ssh://github.com:apiaddicts/openapi2soapui.git</developerConnection>
		<url>https://github.com/apiaddicts/openapi2soapui/tree/master</url>
	</scm>

	<distributionManagement>
		<snapshotRepository>
			<id>ossrh</id>
			<url>https://s01.oss.sonatype.org/content/repositories/snapshots</url>
		</snapshotRepository>
		<repository>
			<id>ossrh</id>
			<url>https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/</url>
		</repository>
	</distributionManagement>


	<modules>
		<module>openapi2soapui-core</module>
		<module>openapi2soapui-rest</module>
		<module>openapi2soapui-cli</module>
	</modules>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<java.version>21</java.version>

		<!-- Default packaging for openapi2soapui-rest; overridden by the jar/INTE/TEST/PROD profiles.
		     Kept as a plain property (not only inside the "war" profile) so activating any other
		     profile (e.g. release) doesn't leave it unresolved. -->
		<packaging.type>war</packaging.type>

		<springdoc.version>2.8.17</springdoc.version>
		<soapui.version>5.9.1</soapui.version>
		<swagger-parser.version>2.1.45</swagger-parser.version>
		<json.version>20260522</json.version>
		<lombok.version>1.18.46</lombok.version>
		<swagger-v2-parser.version>1.0.76</swagger-v2-parser.version>
		<swagger-v2-core.version>1.6.16</swagger-v2-core.version>

		<maven-source-plugin.version>3.3.1</maven-source-plugin.version>
		<maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
		<maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
		<central-publishing-maven-plugin.version>0.8.0</central-publishing-maven-plugin.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>io.swagger</groupId>
				<artifactId>swagger-parser</artifactId>
				<version>${swagger-v2-parser.version}</version>
			</dependency>
			<dependency>
				<groupId>io.swagger</groupId>
				<artifactId>swagger-core</artifactId>
				<version>${swagger-v2-core.version}</version>
			</dependency>
			<dependency>
				<groupId>io.swagger</groupId>
				<artifactId>swagger-models</artifactId>
				<version>${swagger-v2-core.version}</version>
			</dependency>
			<dependency>
				<groupId>io.swagger</groupId>
				<artifactId>swagger-annotations</artifactId>
				<version>${swagger-v2-core.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<profiles>
		<profile>
			<id>jar</id>
			<properties>
				<packaging.type>jar</packaging.type>
			</properties>
		</profile>
		<profile>
			<id>war</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				 <packaging.type>war</packaging.type>
			</properties>
		</profile>
		<profile>
			<id>INTE</id>
			<properties>
                <packaging.type>war</packaging.type>
			</properties>
		</profile>
		<profile>
			<id>TEST</id>
			<properties>
                <packaging.type>war</packaging.type>
			</properties>
		</profile>
		<profile>
			<id>PROD</id>
			<properties>
                <packaging.type>war</packaging.type>
			</properties>
		</profile>
		<profile>
			<!-- Signs artifacts before publishing to Maven Central. Activate with -Prelease. -->
			<id>release</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>${maven-gpg-plugin.version}</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	 </profiles>

	<repositories>
		<repository>
			<id>SmartBearPluginRepository</id>
			<url>https://rapi.tools.ops.smartbear.io/nexus/content/groups/public/</url>
		</repository>
	</repositories>

	<build>
		<plugins>
			<!-- To release to Maven Central -->
			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
				<version>${central-publishing-maven-plugin.version}</version>
				<extensions>true</extensions>
				<configuration>
					<publishingServerId>central</publishingServerId>
					<autoPublish>true</autoPublish>
					<waitUntil>published</waitUntil>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>${maven-source-plugin.version}</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>${maven-javadoc-plugin.version}</version>
				<configuration>
					<doclint>none</doclint>
					<failOnError>false</failOnError>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
